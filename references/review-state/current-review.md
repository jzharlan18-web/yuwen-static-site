# 当前课件审核状态

## 本次任务

- 课文：《中国人民站起来了》（毛泽东 · 开幕词）
- 课堂类型：新课教学（微课 · 课文细读型）
- 来源：教师提供的《10分钟课堂教学逐字稿》
- 生成时间：2026-07-12

## 文件清单

| 文件 | 说明 |
| --- | --- |
| `gaoer/textbook/unit-01/zhongguo-renmin-zhanqilaile/index.html` | 本课演示网页（新建） |
| `gaoer/textbook/unit-01/zhongguo-renmin-zhanqilaile/assets/images/` | 图片目录 |
| `gaoer/textbook/unit-01/zhongguo-renmin-zhanqilaile/assets/images/jiangzhu-gandao.jpg` | 学校主干道照片（小结迁移部分） |
| `gaoer/textbook/unit-01/index.html` | 单元目录（已更新本课链接，原 `#` → 本课文件夹） |
| `tools/whiteboard/index.html` | 共享课堂白板（修复「返回」「更多/保存」问题，本课及全站受益） |

## 预览位置

- 本地预览：`gaoer/textbook/unit-01/zhongguo-renmin-zhanqilaile/index.html`（建议用浏览器打开，或本地静态服务器）
- 一体机宽度：横向 1920×1080 触摸；手机宽度：390px 左右单栏

## 检查结果

### 已核实

- 课堂类型判断：新课教学，非试题讲评、非作文指导。
- 页面模式：单页综合网页（长页面 + 分步揭示），贴合 10 分钟微课体量。
- 原文保真：两句关键引文（第 6 段、第 12 段）及结尾句均逐字照录逐字稿，未杜撰、未改写。
- 结构对应：6 个教学环节与逐字稿一一对应（导入→背景→梳理思路→品读→内涵→小结+迁移）；板书完整呈现。
- 站点规范：复用 `assets/css/style.css`；统一 6 项导航；相对路径按 unit-01 层级正确（首页 `../../../../`、教材课文 `../../../`）。
- 白板接入：按 README 规范加载 `classroom-whiteboard.js`，`data-lesson-title="中国人民站起来了"`。
- 白板红线已写入 `references/review-state/manual-red-lines.md` 第五节「课堂白板接入红线」，本课与白板文件均满足：已接入悬浮入口；返回可关闭白板回课件；「更多/保存」常驻可见；自动保存静默不刷屏。该红线作为后续每次生成网页的必验项。
- 白板修复（共享工具）：①「返回」改为聚焦课件父窗口后关闭白板弹窗，真正回到课件；②工具栏右侧「保存 / 全屏 / 隐藏栏 / 更多」改为 `position:sticky` 常驻可见组，解决窄屏横向滚动时「更多」被挤出、保存等功能的入口丢失问题；并新增常驻「保存」按钮（id=`btnSaveQuick`）一键本机保存；③**自动保存改为静默**：原 `save()` 每次操作都先弹「保存中…」再弹「已保存到当前浏览器」，干扰上课。现拆分为静默 `persist()`（自动保存不弹提示，仅 400ms 防抖写入 localStorage）+ 显式 `doSave()`（仅用户点「保存」按钮时弹确认）。保存频率不变（按完成动作触发、防抖合并），数据安全不受影响。
- 触摸可达：揭示按钮 ≥48px；所有信息可点击/键盘访问，不依赖悬停；关键词静态高亮。
- 投屏可读（按红线第六节）：正文基准放大到 `clamp(1.45rem,3vw,2rem)`（约 23–32px），与栏目标题 `h2` 等值，满足「正文不小于标题」；原文引用 `clamp(1.7rem,3.4vw,2.4rem)`（约 27–38px）；板书 `clamp(1.6rem,3.2vw,2.3rem)`（约 26–37px）；小标题 `h3` `clamp(1.5rem,3vw,2rem)`；均 ≥ 正文。该标准已写入红线第六节，后续每课默认自检。
- 小结迁移部分已加入学校主干道照片，并配迁移性 caption（“今日校园的和平宁静，正是先辈‘站起来’的果实”）。

### 待补

- 开国大典图片：页面留带说明的占位框；教师将图片命名为 `kaidian-dadian.jpg` 放入 `assets/images/` 后自动显示（未擅自引用网络图片）。
- 全文细读视图：逐字稿仅提供两句关键引文；如需全文阅读视图，待教师提供教材原文后补充（本次两句引文已足够支撑微课品读）。

### 内容检查

- HTML 结构完整，揭示按钮 `data-reveal` 与面板 `id` 一一对应（r-title / r-read1 / r-read2 / r-sum）。
- 内联脚本：揭示切换 + 环节导航 IntersectionObserver 高亮，语法自包含。
- 原文块与讲解文字视觉区分（引用块带金色左边框 + 衬线大字；分析用注释色）。

### 交付检查

- 单元目录链接已更新并可跳转。
- 未引入数据库、登录、服务器或框架。
- 已通过账号 `jzharlan18-web` 提交并推送至 GitHub `origin/main`（普通推送，未强制）；`README.md` 与 `docs/` 按计划排除。

## 审核状态：定稿完成

> 已于 2026-07-12 经账号 `jzharlan18-web` 成功推送至 GitHub（`git push origin main`，普通推送）。
> 提交含 8 文件：本课演示网页 + 学校主干道照片 + 单元目录链接 + 共享白板修复 + 白板脚本/样式依赖 + 红线与审核文档；`README.md`、`docs/` 排除，未混入本课提交。
> 白板修复（返回可关、更多/保存常驻、自动保存静默）已随课发布，全站课件受益。
> 待补项保留：① 开国大典图片（放 `kaidian-dadian.jpg` 自动显示）；② 全文细读视图（待提供教材原文）。
